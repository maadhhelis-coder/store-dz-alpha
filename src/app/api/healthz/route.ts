export { GET } from "@/app/api/health/route";

// اكتُشف فعليًا: /api/health يعمل بشكل صحيح محليًا وداخل Docker (نفس خادم standalone الحقيقي)،
// لكن على Vercel تحديدًا يُعيد 404 (يطابق /404 الثابت المخزَّن، وليس الدالة) رغم أن مسارًا آخر
// من نفس البنية (/api/wilayas) يعمل بلا مشكلة عبر نفس آلية trailingSlash — لم يُعثر على أي فرق
// فالكود أو الإعدادات يفسّر هذا (لا vercel.json خاص، لا middleware يستثنيه، لا مسار pages/api
// قديم متعارض). بدل تغيير trailingSlash عالميًا (خطر حقيقي على Docker/E2E المُثبَتين فعليًا)،
// هذا مسار بديل جديد كليًا (لم يمرّ بأي بناء/تخزين مؤقت سابق) يُستعمل الآن كنقطة مراقبة خارجية
// (UptimeRobot) بدل /api/health على الإنتاج تحديدًا.
